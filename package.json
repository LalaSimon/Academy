{
  "name": "academy",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "test:e2e": "playwright test",
    "format": "turbo format"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "serve": "^14.2.4",
    "turbo": "^2.5.4",
    "wait-on": "^8.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
